# IMS Service Local Development Environment Variables

# Server Configuration
PORT=3001
NODE_ENV=development
LOG_LEVEL=debug

# Authentication Configuration
USE_LOCAL_AUTH=true

# AWS Configuration (using IAM role)
AWS_REGION=us-east-1
# AWS credentials will be obtained from:
# 1. IAM instance profile (if running on EC2)
# 2. AWS CLI credentials (~/.aws/credentials)
# 3. Environment variables (if needed for specific use cases)
# 4. IAM roles for service accounts (if running on EKS)

# Cognito Configuration
USER_POOL_ID=us-east-1_W485ARcfx
USER_POOL_CLIENT_ID=4tj315iifb3b8hk0n1cd6agpce
USER_POOL_CLIENT_SECRET=1b7hftmu2qstro157ht5u5j4mm4hjaqd8vqvrh2de2mnaf1qdld4
USER_POOL_REGION=us-east-1

# DynamoDB Configuration
TENANT_REGISTRY_TABLE_NAME=admin-portal-dev-tenant-registry
DYNAMODB_ENDPOINT=https://dynamodb.us-east-1.amazonaws.com

# CORS Configuration
CORS_ORIGINS=http://localhost:3000,http://localhost:3001,http://127.0.0.1:3000
ALLOWED_METHODS=GET,POST,PUT,DELETE,OPTIONS
ALLOWED_HEADERS=Content-Type,Authorization,X-Requested-With

# JWT Configuration
JWT_SECRET=your-jwt-secret-for-local-dev
JWT_SIGNING_KEY=your-jwt-secret-for-local-dev
JWT_EXPIRES_IN=24h

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100