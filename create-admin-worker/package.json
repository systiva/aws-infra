{
  "name": "create-admin-worker",
  "version": "1.0.0",
  "description": "Lambda function to create account admin user during onboarding",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "local": "set ENV=local&& set AWS_ACCESS_KEY_ID=fake&& set AWS_SECRET_ACCESS_KEY=fake&& set AWS_DEFAULT_REGION=us-east-1&& node ./index.js | npx pino-pretty"
  },
  "dependencies": {
    "@aws-sdk/client-lambda": "^3.600.0",
    "pino": "^9.5.0"
  },
  "devDependencies": {},
  "keywords": [
    "aws",
    "lambda",
    "account",
    "admin",
    "user-creation"
  ],
  "author": "Multi-Account Platform",
  "license": "MIT"
}